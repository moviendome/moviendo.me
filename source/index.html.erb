---
title: Building with Web3 & React | Based in Thailand
pageable: true
per_page: 20
---

<% highligted = page_articles[0] %>
    <div class="container">
      <div class="content is-medium">
        <h6 class="subtitle is-7 is-uppercase"><time><%= highligted.date.strftime('%b %e, %Y') %></time></h6>
        <a href="<%= highligted.url %>">
          <h4><%= highligted.title %></h4>
        </a>

        <%= highligted.summary %>

        <% if highligted.data.ig.present? %>
          <p class="has-text-centered mt-6">
            <a href="<%= highligted.data.ig %>" target="_blank">
              <%= image_tag('icons/instagram.svg', width: '30px') %>
            </a>
          </p>

        <% else %>

          <div class="has-text-centered mt-6">
            <a href="https://twitter.com/moviendo_me" target="_blank">
              <%= image_tag('icons/twitter.svg', width: '30px') %>
            </a>
          </div>

        <% end %>
      </div>
    </div>

<hr />

<% page_articles.drop(1).each do |article| %>
  <% unless article.tags.include?("draft") %>
    <div class="post">
      <h6 class="subtitle is-7 is-uppercase"><time><%= article.date.strftime('%b %e, %Y') %></time></h6>

      <!--
      <div class="tags">
        <% article.tags.each do |tag| %>
          <span class="tag is-light is-uppercase"><%= tag %></span>
        <% end %>
      </div>
      -->

      <h4>
        <a href="<%= article.url %>">
          <%= article.title %>
        </a>
      </h4>
      <hr />
    </div>
  <% end %>
<% end %>

<!--
<% if paginate %>
  <nav class="pagination is-centered" role="navigation" aria-label="pagination">
    <ul class="pagination-list">
      <% if prev_page %>
        <%= link_to 'Previous', prev_page, class: "pagination-previous" %>
      <% end %>
      <% if next_page %>
        <%= link_to 'Next page', next_page, class: "pagination-next" %>
      <% end %>
    </ul>
  </nav>
<% end %>
-->

<div class="has-text-centered mt-6">
  <a href="https://twitter.com/moviendo_me" target="_blank">
    <%= image_tag('icons/twitter.svg', width: '30px') %>
  </a>
</div>
